# dependencies
node_modules/
# 락파일은 package-lock.json 하나만 커밋한다 — 둘이 어긋나면 설치 결과가 갈린다
pnpm-lock.yaml
yarn.lock

# 로컬 설정 (토큰 등 민감정보 포함 가능)
config.json

# macOS
.DS_Store

# 로그/임시
*.log
.dev/
dist/
out/

# Python 캐시
__pycache__/
*.pyc
