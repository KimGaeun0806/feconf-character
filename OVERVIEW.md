# 화면 구석에 상주하는 빌드·테스트 상태 알림 달팽이

빌드와 테스트 상태를 화면 구석에서 실시간으로 알려주는 Electron 데스크톱 캐릭터입니다.

## 기본 제공 구성

- **감정 애니메이션 9종**
  greet · happy · surprise · love · curious · peek · wake · sleep · walk
- **감정별 short · long 버전 — 총 애니메이션 JSON 18개**
  앱은 주로 `*-long` 을 재생하며, short 는 프레임 절반의 축약판입니다
- **픽셀 말풍선 3종**
  comic · purple · cozy
- **픽셀 폰트**

## 자체 캐릭터 포맷

캐릭터는 이미지 파일이 아니라 36×22 색상 그리드 JSON으로 구성됩니다.

- JSON 1페이지가 애니메이션 1프레임에 해당합니다.
- 각 셀은 -20° 기울어진 둥근 마름모 블록으로 렌더링됩니다.
- JSON을 직접 수정하거나 스크립트로 새로운 캐릭터와 애니메이션을 생성할 수 있습니다.

## 파일 이름이 곧 API

애니메이션은 다음 네이밍 규칙을 사용합니다.

`snail-wide-<emotion>[-long].json`

파일 이름과 포맷만 맞추면 별도의 코드 수정 없이 캐릭터 애셋 전체를 교체할 수 있습니다.
애셋은 [`character/`](character/) 폴더에 있습니다.

## 확장 가능한 픽셀 말풍선

말풍선도 캐릭터와 동일한 JSON 포맷을 사용합니다 (`bubble-*.json`).

텍스트 길이에 따라 중앙 컬럼을 반복 복제하는 방식으로 크기가 늘어나기 때문에, 말풍선을 확장해도 픽셀 그래픽이 깨지지 않습니다.

## 외부 도구 연동

`localhost:7842` 웹훅을 통해 어떤 도구와도 연결할 수 있습니다.

- `/notify` — 알림
- `/state` — 상태 전환
- `/activity` — 코딩 활동
- `/vitals` — Core Web Vitals 실측값 전달 (기준 이내면 love, 넘기면 curious)

## 메인 프로세스 구조

진입점은 [`main.js`](main.js) 이고, 설정·애셋 로드·vitals·웹훅은 [`lib/`](lib/) 로 나뉩니다.

자세한 제작 규칙과 스펙은 [CUSTOMIZING.md](CUSTOMIZING.md)를 참고하세요.
